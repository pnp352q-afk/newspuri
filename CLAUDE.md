# 뉴스풀이 — 프로젝트 안내

Claude Code 가 이 폴더에서 작업할 때 읽는 규칙서. «언제 무엇을 왜 만들었나»는 `docs/00-구현기록.md`.

## 이 서비스가 뭔가

주가에 영향을 주는 공시·뉴스·경제지표를 모아 일반인이 알아듣게 푼다:
① 무슨 일인가 ② 왜 중요한가(호재 쪽/악재 쪽/중립/판단 어려움 + 이유) ③ 과거 비슷한 일 뒤 1·5·20일 움직임(시장 대비, 중앙값)
④ 영향이 번질 회사·업종(3단계) ⑤ 시장 심리 온도계(2단계).
**예측이 아니라 해석 서비스다.** «내일 오른다/내린다»를 단정하지 않는다.

## 사용자에 대해

- 비개발자 기획자. 설명은 한국어로, 초등학생도 알아듣게. 전문용어 옆에 쉬운 말.
- 터미널 명령은 복사해 붙여넣을 한 줄 + 어디에 붙이는지. 여러 단계는 한 번에 한 단계씩, 끝날 때마다 확인받는다.
- 코드를 고치기 전에 무엇을 왜 바꾸는지 한 줄 먼저. 단계가 끝나면 브라우저로 화면을 확인하고 무엇을 확인했는지 말한다.

## 절대 규칙

1. 모든 화면에 **«투자 자문이 아닙니다»** 안내 유지. 매수·매도 권유 문장 금지(`config/forbidden.csv`).
2. 수집은 **허락 대장** `config/sources.csv` 를 지난다. allow=예 인 곳만. 대장에 없으면 멈춘다(`src/lib/ledger.js`).
3. 뉴스 원문 재게시 금지 — 제목·요약·링크만.
4. 모든 숫자에 출처와 기준 시각. 모르면 «확인 필요». 지어내지 않는다. **표본 30건 미만은 값을 싣지 않는다.**
5. LLM 해석에는 근거(원문 글자 그대로의 구절 + 원문 링크)가 반드시 있다. 원문에 없는 근거면 저장하지 않는다.
6. 무거운 계산(사건 연구·모델 학습)은 PC 에서, 서버는 결과(`web/today.json`)만 보여 준다.
7. API 열쇠·비밀번호는 코드·로그·대화에 값으로 쓰지 않는다. 열쇠는 `C:\keys\.dartkey` 같은 파일에서만(`src/lib/keys.js`, 메모장이 붙인 `.txt` 도 받는다). 사용자 계정에 대신 로그인하지 않는다.
8. 예측 모델은 미래 정보가 섞이지 않게(워크포워드 검증, 발표 시각 기준 정렬) 재고, 성적이 나빠도 그대로 기록한다.
9. `docs/법률확인.md` 를 전문가에게 확인하기 전에는 유료화하지 않는다.

## 흐름

```
fetch_dart.js → data/news.db(disclosure) → interpret.js(Claude, 공시마다 한 번, 원문도 document 표에 보관)
import_prices.js(허락된 주가) → event_study.js → event_stats
build_web.js → web/today.json → server.js(127.0.0.1:3320) → web/index.html
check.js — 배포 전 반드시 [점검 통과]. 공개 배포는 check.js --deploy (견본 카드가 있으면 실패)
```

- 견본(가짜) 자료는 `fixtures/`, DB 의 `source='sample'`. **실제 자료와 섞지 않는다** — 실제 공시에 견본 통계가 붙으면 점검이 떨어뜨린다.
- 표(`config/*.csv`)는 기획자가 고치는 정본이다. 코드에 같은 목록을 따로 박지 않는다.
- `data/*.db` 는 git 에 넣지 않는다(다시 만들 수 있다).

## 환경의 함정

- DART 제목의 가운뎃점은 «ㆍ»(U+318D) — 분류할 때 «·»·띄어쓰기·괄호를 지우고 견준다(`src/lib/classify.js`).
- DART 응답은 `status` 를 반드시 본다. 020(요청 한도)은 «0건»이 아니다 — 쉬고 다시, 그래도 안 되면 멈추고 «받은 칸 N/M» 을 적는다.
- 오늘 날짜의 공시 목록은 계속 늘어난다 — 이어받기 장부에 «끝냈다»고 적지 않는다.
- `node:sqlite` 는 Node 22.5 이상. «ExperimentalWarning» 은 무해하다(npm 스크립트가 숨긴다).
